apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: comments
spec:
  rules:
    - host: comments-wrichter-dhbw-exercise-1.apps.osd-evalcluster.v0z2.p1.openshiftapps.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: comments
              servicePort: 4001
